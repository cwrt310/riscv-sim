# 待办事项（riscv-sim）

> 更新日期：2026-08-31

## 🔴 明天先做：修 Qt 界面两个 bug

### 1. 未实现指令会闪退（没 try-catch）
- **现象**：输入没实现的指令，界面直接关掉，不提示错误。
- **原因**：Assembler/CPU 遇到未知指令会 `throw`，异常在按钮槽函数里抛出，Qt 不接 → 崩溃。
- **修法**：`load` / 运行按钮 / 单步按钮里加 `try-catch`，捕获 `std::exception`，把 `e.what()` 显示到日志。

### 2. 看不到内存（内存表 0 行 + 只显示代码段）
- **现象**：内存表空白，或 sw 写的数据（如地址 100）看不到。
- **原因**：内存表初始 0 行，且只显示「程序代码」这一段（地址 0 ~ 程序末尾）。
- **修法**：改成固定 32 行，显示前 128 字节（地址 0x00 ~ 0x7C），代码段 + 数据段都能看到。

## 🟡 待办

### 3. 访存指令测试验证（lw/sw/lb/lh/sb/sh 已写完，还没测）
- [ ] sw + lw 配对：`sw x5, 0(x2)` 后 `lw x6, 0(x2)`，x6 应该是 42
- [ ] sb + lb 符号扩展：`sb x5, 0(x2)`（x5=-1）后 `lb x6, 0(x2)`，x6 应该是 0xFFFFFFFF

### 4. 帮助弹窗更新
- [ ] 帮助按钮 QMessageBox 更新成 19 条指令（加「访存·读」「访存·写」，删「还没实现」里的 lw/sw）

### 5. 文档更新（访存进度 13 → 19 条）
- [ ] `RV32I指令集详解.md` 进度更新 + 访存指令编码补详细
- [ ] `README.md` 已实现 13 → 19 条
- [ ] `00_学习进度.md` 最近活动补访存记录
