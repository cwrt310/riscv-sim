# 待办事项（riscv-sim）

> 更新日期：2026-09-05

## ✅ 已完成（存档，按完成时间倒序）

### 数据通路可视化（09-05）
- [x] 修队友的 `(pc/4)%7` 伪高亮 → 改成按 opcode 的位掩码 `datapathMask()`
- [x] 运行改 QTimer 驱动（一拍一条），灯效可见；「暂停」按钮可用
- [x] 数据通路图重画：6 部件一行 + 写回总线（下方实线）+ PC 跳转回路（上方虚线）
- [x] 速度控件：滑条（毫秒）→ 输入框（条/秒，0.2~50）
- [x] **修「只亮最后一条指令」** → `update()` 换 `repaint()`（详见《调试方法论》）
- [x] 加自证机制：每拍日志 `[N] 助记符` + 图顶「第 N 拍：指令（PC → 0x..）」
- [x] `describeInst()` 机器码反汇编成助记符
- [x] Debug 版编译通过，零警告

### 早期（08-30 ~ 08-31）
- [x] 未实现指令闪退 → `loadProgram`/`onStepClicked`/`onTimerTick` 三处加 `try-catch`
- [x] 内存表 0 行 → `setRowCount(code.size())` 动态；每拍改成复用单元格不再 new
- [x] 单步越界崩溃 → CPU 加 `finished()`
- [x] 帮助弹窗更新成 19 条指令（补访存·读/写）
- [x] `README.md` 已实现 13 → 19 条 + 界面功能 + 文档导航

---

## 🔴 立刻做

### 1. 实际跑一遍验灯（改完还没上手测）
- [ ] 按《测试手册_斐波那契与指令体检.md》走一遍 4 个测试（`tests/*.asm` 已备好，预期值已用命令行版验证过）
- [ ] 速度填 `0.5`，运行 `tests/fib.asm`，确认灯**逐条**走（不是只闪最后一次）
- [ ] 日志区应刷出 58 行 `[N] 助记符`；图顶部「第 N 拍」数字在跳
- [ ] `sw` → 数据存储器亮但**写回总线不亮**（store 不写寄存器）
- [ ] `beq` / `jal` → 上方虚线（PC 回路）变橙

### 2. 访存指令测试验证
- [x] **命令行版已验证通过**（2026-09-05）：sw+lw 配对 x6=42 ✓；sb+lb 符号扩展 x7=0xFFFFFFFF ✓；sh+lh x8=0xFFFFFFFE / x9=300 ✓；小端序 x10=0x44、x11=0x33 ✓
- [ ] Qt 界面里实跑一遍（用 `tests/memory.asm`，预期见《测试手册》）

### 3. git 提交
- [ ] 4 个代码文件：`CPU.h` `CPU.cpp` `mainwindow.h` `mainwindow.cpp`
- [ ] 文档：新增《调试方法论_一个bug的完整复盘.md》，改动 README / 踩坑记录 / Qt 学习笔记 / 项目状态速览 / 本文件

---

## 🟡 待办

### 4. 汇编器补齐（CPU 已实现但汇编器不认）
- [ ] `sll` `srl` `or` `and` `xor` `srli` —— CPU.cpp 里有 case，汇编器加上就白捡 6 条
- [ ] 加完同步：帮助弹窗、README、`RV32I指令集详解.md`

### 5. 文档同步
- [ ] `RV32I指令集详解.md`：访存指令（I/S 型）编码补详细拆解
- [ ] `桌面/学习/00_学习进度.md`：补 09-05 数据通路可视化记录

### 6. 加分项候选
- [ ] 标签跳转（`loop:`）——比伪指令更实用，斐波那契就不用手算偏移了
- [ ] 伪指令 `mv` / `li` / `j` / `nop`
- [ ] `jalr` / `auipc`（配合标签能做函数调用）

---

## 🔵 想做但要评估

- **流水线动画**：现在是**单周期模型**（一条指令一拍走完全部部件），做不出真流水线。要做得先把 CPU 改成多周期或五级流水线——**工作量大，且会推翻现有数据通路图的语义**。建议放「展望」章节讲思路，不实做。
- **控制信号可视化**：在数据通路图上标出 `RegWrite`/`MemRead`/`ALUOp` 等控制信号的取值。比流水线动画简单得多，教学价值也高，性价比更好。
