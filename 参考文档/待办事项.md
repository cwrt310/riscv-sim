# 待办事项（riscv-sim）

> 更新日期：2026-09-05

## 🗺️ 版本路线图（总览）

```
V3.0  表驱动重构 + 补全指令集 + 伪指令/标签
V3.5  特权层（CSR + ecall/mret + trap）→ 汇编版最小内核
V4.0  迷你编译器前端（表达式 → 变量 → if/while）
V4.5  自举彩蛋：自家编译器编译内核的 C 部分
```

排序逻辑见《报告素材_展望_指令算子化.md》〇节——核心一句话：**汇编内核不需要编译器，C 内核必须等编译器，所以 OS 先行、编译器殿后**。

---

## ✅ 已完成（存档，按时间倒序）

### 数据通路可视化（09-05）
- [x] 修队友的 `(pc/4)%7` 伪高亮 → 按 opcode 的位掩码 `datapathMask()`
- [x] 运行改 QTimer 驱动（一拍一条），灯效可见；「暂停」按钮可用
- [x] 数据通路图重画：6 部件一行 + 写回总线（下方实线）+ PC 跳转回路（上方虚线）
- [x] 速度控件：滑条（毫秒）→ 输入框（条/秒，0.2~50）
- [x] 修「只亮最后一条指令」 → `update()` 换 `repaint()`
- [x] 加自证机制：每拍日志 `[N] 助记符` + 图顶「第 N 拍：指令（PC → 0x..）」
- [x] `describeInst()` 机器码反汇编成助记符
- [x] Debug 版编译通过，零警告

### 测试与文档（09-05）
- [x] 测试手册 + `tests/` 4 个程序（fib/memory/all19/datapath_lights），预期值全部命令行验证通过
- [x] 访存 6 指令命令行验证：sw+lw、sb+lb 符号扩展、sh+lh、小端序 ✓
- [x] 《调试方法论》《发布Release流程》两篇新文档
- [x] 13 份文档整理进 `参考文档/`，README 重写（19 条指令表 + 文档导航）
- [x] `make_release.sh` 一键发布脚本（已实测）
- [x] 踩坑记录补发布与文档管理阶段 3 坑（13/14/15）

### 早期（08-30 ~ 08-31）
- [x] 未实现指令闪退 → 三处 `try-catch`
- [x] 内存表 0 行 → `setRowCount(code.size())` 动态；每拍复用单元格
- [x] 单步越界崩溃 → CPU 加 `finished()`
- [x] 帮助弹窗更新成 19 条指令

---

## 🔴 立刻做（发布 V2.0 收尾）

- [ ] **网页发布 V2.0**：`github.com/cwrt310/riscv-sim/releases` → tag `V2.0`（Create new tag on publish）→ 标题 `V2.0 数据通路可视化版` → 描述用之前给的模板 → 拖入桌面 `riscv-sim-V2.0-win64.zip` → Publish
- [ ] 发布后补《发布Release流程.md》第七节档案表 V2.0 行
- [ ] **Qt 界面实跑 4 个测试**（`tests/*.asm`，预期见测试手册）：速度填 `0.5` 看灯逐条走、日志 58 行、`sw` 无写回、`beq`/`jal` 虚线变橙

---

## 🟡 V3.0：表驱动 + 补全指令集 + 伪指令/标签

> 概念材料：嵌入式C 第 2 章（体系结构）；方法参考《报告素材》方向 1/2/4

### 3.1 CPU 侧算子化分派表
- [ ] `using Handler = void(*)(CPU&, u32);` + `Handler dispatch[128];`，把 execute() 的 switch 拆成算子函数登记进表
- [ ] 现有 19 条指令迁移进表

### 3.2 补全 RV32I（缺 11 条 + 白捡 6 条）
- [ ] 缺的 11 条：`jalr` `auipc` `sra` `slt` `sltu` `lbu` `lhu` `blt` `bge` `bltu` `bgeu`
- [ ] 白捡 6 条：`sll` `srl` `or` `and` `xor` `srli`（CPU.cpp 已有 case，汇编器同步即可）
- [ ] 完成后 RV32I **37 条全齐**，帮助弹窗/README 同步
- [ ] 分工建议：每人领一组指令（表驱动天然防文件冲突）

### 3.3 汇编器表驱动
- [ ] `InsnDesc`（opcode/funct3/funct7/type）+ `insn_table` 替换 if-else 链
- [ ] 伪指令：`mv` `li` `j` `nop` `not` `neg` `beqz` `la`
- [ ] 标签：两遍扫描（第一遍收集标签地址，第二遍解析引用）——斐波那契不用再手算偏移

### 3.4 V3.0 验收
- [ ] 37 条指令 + 伪指令全部过测试（可对拍 Spike 模拟器）
- [ ] 标签版斐波那契跑通（`loop:` / `j loop` 写法）

---

## 🟡 V3.5：特权层 + 汇编版最小内核（OS 第一步）

> 概念材料：嵌入式C 第 10 章 10.1~10.4 + 汪辰《循序渐进》教程第 1~4 讲（QEMU 上照做，机制移植进本模拟器，注意 RV64→RV32 差异）；结构参考：南大 PA nanos-lite（riscv32-nemu 版）

- [ ] CSR 寄存器：`mstatus` `mtvec` `mepc` `mcause` `mtval`（读/写指令 `csrrw/csrrs/csrrc`）
- [ ] 特权指令：`ecall` `mret`
- [ ] trap 流程：异常 → 存 mepc/mcause → 跳 mtvec 指向的 handler → `mret` 返回
- [ ] 假 UART：向 MMIO 地址写 1 字节 → Qt 日志区打印
- [ ] **最小内核（汇编 ~20 行）**：启动打印 → 读字符 → 回显 → 循环
- [ ] 验收：Qt 界面运行"内核"，日志区出现内核输出 → **报告卖点："模拟器跑出了操作系统"**

---

## 🟡 V4.0：迷你编译器前端（超纲加分）

> 材料：《报告素材》方向 3；对应你 MLIR 长线方向的入门台阶

- [ ] 分层推进：算术表达式（四则+括号）→ 变量与赋值（符号表）→ `if`/`while` 控制流
- [ ] 组件：词法分析 → 语法分析 → 代码生成（生成汇编文本喂给现有汇编器）
- [ ] 验收：`x = 3-(5-2); while(x<10) x = x+1;` 跑通
- [ ] 分工建议：1 人主力，其余人写测试用例

---

## 🟡 V4.5：自举彩蛋（可选）

- [ ] 用 V4.0 编译器编译内核的 C 部分 → 模拟器跑**自家编译器产出的内核**
- [ ] 报告叙事闭环：模拟器 → 特权架构 → 自家编译器 → 自己编译自己的 OS

---

## 🔵 想做但要评估

- **流水线动画**（展望方向 6 剩余部分）：现在是**单周期模型**，一条指令一拍走完全部部件，做不出真流水线。要做得先把 CPU 改成多周期/五级流水线——大工程，会推翻现有数据通路图语义。建议只在报告里讲思路，不实做。
- **控制信号可视化**：在数据通路图上标 `RegWrite`/`MemRead`/`ALUOp` 等控制信号取值。比流水线动画简单得多，教学价值高，可作为 V3.x 间隙的小加分项。
- **对拍 Spike**：装官方 Spike 模拟器，把测试程序两边跑对拍（展望结语提过）。V3.0 验收的加分做法。
