# 待办事项（riscv-sim）

> 更新日期：2026-09-05

## 🗺️ 版本路线图（总览）

```
V3.0  表驱动重构 + 补全指令集 + 伪指令/标签
V3.5  特权层（CSR + ecall/mret + trap）→ 汇编版最小内核
V4.0  迷你编译器前端（表达式 → 变量 → if/while）
V4.5  自举彩蛋：自家编译器编译内核的 C 部分
```

排序逻辑见《报告素材_展望_指令算子化.md》〇节——核心一句话：**汇编内核不需要编译器，C 内核必须等编译器，所以 OS 先行、编译器殿后**。

---

## ✅ 已完成（存档，按时间倒序）

### 数据通路可视化（09-05）
- [x] 修队友的 `(pc/4)%7` 伪高亮 → 按 opcode 的位掩码 `datapathMask()`
- [x] 运行改 QTimer 驱动（一拍一条），灯效可见；「暂停」按钮可用
- [x] 数据通路图重画：6 部件一行 + 写回总线（下方实线）+ PC 跳转回路（上方虚线）
- [x] 速度控件：滑条（毫秒）→ 输入框（条/秒，0.2~50）
- [x] 修「只亮最后一条指令」 → `update()` 换 `repaint()`
- [x] 加自证机制：每拍日志 `[N] 助记符` + 图顶「第 N 拍：指令（PC → 0x..）」
- [x] `describeInst()` 机器码反汇编成助记符
- [x] Debug 版编译通过，零警告

### 测试与文档（09-05）
- [x] 测试手册 + `tests/` 4 个程序（fib/memory/all19/datapath_lights），预期值全部命令行验证通过
- [x] 访存 6 指令命令行验证：sw+lw、sb+lb 符号扩展、sh+lh、小端序 ✓
- [x] 《调试方法论》《发布Release流程》两篇新文档
- [x] 13 份文档整理进 `参考文档/`，README 重写（19 条指令表 + 文档导航）
- [x] `make_release.sh` 一键发布脚本（已实测）
- [x] 踩坑记录补发布与文档管理阶段 3 坑（13/14/15）

### 早期（08-30 ~ 08-31）
- [x] 未实现指令闪退 → 三处 `try-catch`
- [x] 内存表 0 行 → `setRowCount(code.size())` 动态；每拍复用单元格
- [x] 单步越界崩溃 → CPU 加 `finished()`
- [x] 帮助弹窗更新成 19 条指令

---

## ✅ V2.0 发布收尾（已完成）

- [x] **网页发布 V2.0** ✅（tag `32f9650`，用户已发布）
- [x] 《发布Release流程.md》第七节档案表已补 V2.0 行
- [x] Qt 界面实跑测试：`all19.asm`（23 条 ✓）+ `datapath_lights.asm`（7 条 ✓）已通过
- [ ] 补跑 `fib.asm`（58 行、x2=34/x3=55）和 `memory.asm`（19 条访存）——顺手做

---

## 🟡 V3.0：表驱动 + 补全指令集 + 伪指令/标签

> 📘 **动手前先读**：《表驱动入门_从函数指针到哈希表.md》（语法）→《表驱动改造实战_riscv-sim版.md》（施工图）
> 🤝 **分工见《团队分工_当前阶段.md》**（已重写：组长先合骨架 → 队友并行补指令）
> ⚠️ **顺序红线：组长先做完两张表骨架并合入 main，队友再开工**，否则照样冲突

### 3.1 汇编器表驱动 ✅ 完成（2026-09-07）
- [x] **第 1 步**：`InsnDesc` 结构体 + `insnTable` 25 行全登记
- [x] **第 2 步**：`encodeI` / `encodeR`（namespace 内）
- [x] **第 3 步**：查表路径接入，与旧 if 链并存对拍通过
- [x] **第 4 步**：7 个编码器 + 7 个 format 分支（B 型 `>>1`、M/S 拆括号，坑 16/17）；**旧 if 链删除**（assembleLine 230 → 50 行）；`encodeM` 删除（M 复用 encodeI）
- [x] **第 5 步**：`encodeI` 加立即数范围检查（修踩坑记录 9b 截断 bug）+ 补 funct7 支持（srai 用）

### 3.2 CPU 侧算子化分派表 ✅ 完成（2026-09-07）
- [x] `using handler = void(*)(CPU&, u32);` + `handlers[128]` + `initHandlers()`（for 循环全填兜底再登记 7 个）
- [x] 7 个算子（executeOP_IMM/OP/LUI/BRANCH/JAL/LOAD/STORE）+ executeILLEGAL 兜底
- [x] 3 个 setter 窗口（setreg/setpc/getmem）解决 private 访问
- [x] `execute()` 缩成两行（查表 + 调用）；旧 130 行 switch 删除
- [x] 踩坑：BRANCH/JAL 丢 -4、`= {executeILLEGAL}` 只填第一格（坑 21/22）
- [x] 编译零警告；4 个 tests 回归通过（以测试手册为准）

> 🎉 **两张表骨架全部就位，顺序红线第一阶段完成——队友可以并行开工补 11 条指令了**

### 3.3 补全 RV32I ✅ 37 条全齐（2026-09-12）
- [x] ~~白捡 6 条~~：`sll` `srl` `or` `and` `xor` `srli`（队友 PR #4）
- [x] 11 条新指令：`jalr` `auipc` `sra` `slt` `sltu` `lbu` `lhu` `blt` `bge` `bltu` `bgeu`（队友 PR #7/#8 合并，**但有错误**，见下）
- [x] `srai`（队友漏的，组长补：表加行 + encodeI 补 funct7 支持）
- [x] **组长修复队友 PR 的全部错误**（见踩坑记录 23 号）：汇编器表 5 处 + CPU 侧 6 处 + srai 2 处
- [ ] 编译 + tests 全量回归（**等 E 盘/PSSD 插回**）
- [ ] 帮助弹窗/README/测试手册同步 37 条
- [ ] 伪指令 7 条（`mv` `li` `j` `nop` `not` `neg` `beqz`）——队友 2 未交

### 3.4 标签跳转（组长）
- [ ] 两遍扫描：第一遍收集标签地址，第二遍解析引用
- [ ] 标签版斐波那契跑通（`loop:` / `j loop` 写法）

### 3.5 V3.0 验收
- [x] RV32I **37 条全齐**（代码层面）
- [ ] 37 条 + 伪指令全部过测试（可对拍 Spike 模拟器）
- [x] if 链消失、execute() 两行
- [ ] 标签版斐波那契跑通
- [ ] 队友 README patch-4 的 `s1l` 错别字修复（PR 合并）
- [ ] 伪指令 7 条

---

## 🟡 V3.5：特权层 + 汇编版最小内核（OS 第一步）

> 概念材料：嵌入式C 第 10 章 10.1~10.4 + 汪辰《循序渐进》教程第 1~4 讲（QEMU 上照做，机制移植进本模拟器，注意 RV64→RV32 差异）；结构参考：南大 PA nanos-lite（riscv32-nemu 版）

- [ ] CSR 寄存器：`mstatus` `mtvec` `mepc` `mcause` `mtval`（读/写指令 `csrrw/csrrs/csrrc`）
- [ ] 特权指令：`ecall` `mret`
- [ ] trap 流程：异常 → 存 mepc/mcause → 跳 mtvec 指向的 handler → `mret` 返回
- [ ] 假 UART：向 MMIO 地址写 1 字节 → Qt 日志区打印
- [ ] **最小内核（汇编 ~20 行）**：启动打印 → 读字符 → 回显 → 循环
- [ ] 验收：Qt 界面运行"内核"，日志区出现内核输出 → **报告卖点："模拟器跑出了操作系统"**

---

## 🟡 V4.0：迷你编译器前端（超纲加分）

> 材料：《报告素材》方向 3；对应你 MLIR 长线方向的入门台阶

- [ ] 分层推进：算术表达式（四则+括号）→ 变量与赋值（符号表）→ `if`/`while` 控制流
- [ ] 组件：词法分析 → 语法分析 → 代码生成（生成汇编文本喂给现有汇编器）
- [ ] 验收：`x = 3-(5-2); while(x<10) x = x+1;` 跑通
- [ ] 分工建议：1 人主力，其余人写测试用例

---

## 🟡 V4.5：自举彩蛋（可选）

- [ ] 用 V4.0 编译器编译内核的 C 部分 → 模拟器跑**自家编译器产出的内核**
- [ ] 报告叙事闭环：模拟器 → 特权架构 → 自家编译器 → 自己编译自己的 OS

---

## 🔵 想做但要评估

- **流水线动画**（展望方向 6 剩余部分）：现在是**单周期模型**，一条指令一拍走完全部部件，做不出真流水线。要做得先把 CPU 改成多周期/五级流水线——大工程，会推翻现有数据通路图语义。建议只在报告里讲思路，不实做。
- **控制信号可视化**：在数据通路图上标 `RegWrite`/`MemRead`/`ALUOp` 等控制信号取值。比流水线动画简单得多，教学价值高，可作为 V3.x 间隙的小加分项。
- **对拍 Spike**：装官方 Spike 模拟器，把测试程序两边跑对拍（展望结语提过）。V3.0 验收的加分做法。
