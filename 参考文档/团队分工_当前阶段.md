# 团队分工（当前阶段）

> 状态：13 条指令 + Qt 界面 + 斐波那契已跑通（第一阶段完成），进入"补访存/跳转 + 打磨报告"阶段。
> 目标：**补全 lw/sw、jalr、auipc 等剩余指令 + 完善测试与报告**。
>
> **完成记录**：目前**所有已完成的工作（13 条指令 + 汇编器 + 斐波那契测试 + Qt 界面 + 全部文档）均由架构负责人（你）独立完成**。下表是后续模块的负责人安排，未完成部分才由对应队员接手。

---

## 分工表

| 人 | 负责 | 难度 | 产出 |
|---|---|---|---|
| **架构负责人**（你） | 分支指令 + 集成 + 报告 | ⭐⭐⭐ | `beq/bne/blt/bge` + 斐波那契测试 + 报告主笔 |
| **队员 B** | 逻辑 + 移位指令 | ⭐ | `and/or/xor/sll/srl/srli`（待补） |
| **队员 C** | 访存指令 | ⭐⭐ | `lw/sw/lb/sb` |
| **队员 D** | Qt 界面 | ⭐⭐ | 数据通路图 / 流水线动画（加分项） |

> 跳转 `jal/jalr`：谁先空出来谁补，或架构负责人补。

---

## 加指令的统一套路（所有人必读）

**加一条指令，永远改两个文件**（详见 `给模拟器加一条指令_andi实例.md`）：

1. `CPU.cpp` 的 `execute()` 里加一个 `case`（让 CPU 会执行）；
2. `Assembler.cpp` 的 `assembleLine()` 里加一个 `if (mnemonic == "xxx")` 块（让汇编器会翻译）。

**踩坑提醒**：打包时 `(u32(imm) & 0xFFF) << 20` 的**括号不能省**（`<<` 优先级高于 `&`）。

---

## 三条铁律（避免打架）

1. **接口冻结**：`.h` 文件（`common.h`/`CPU.h`/`Memory.h`/`RegisterFile.h`/`Assembler.h`）**不许改**，要改必须全组同步；
2. **每人一个 git 分支**：`feat/逻辑指令`、`feat/访存`、`feat/界面`……合并走 PR；
3. **勤同步**：动手前先 `git pull`，改完立刻 `git push`，避免 `CPU.cpp` 冲突。

---

## 关键接口（界面同学只需要知道这些）

界面只依赖 CPU 的 6 个接口，**不碰核心逻辑**：

| 类型 | 接口 | 界面对应 |
|---|---|---|
| 控制 | `loadProgram()` / `step()` / `run()` | "加载"/"单步"/"运行"按钮 |
| 观察 | `pc()` / `reg(n)` / `mem()` | 代码面板 / 寄存器面板 / 内存面板 |

---

## 里程碑

| 周 | 目标 |
|---|---|
| 第 1 周 | 架构攻 `beq`（解锁循环）+ 开组会认领分工 + 各模块起步 |
| 第 2 周 | 指令补齐 ≥10 条 + 斐波那契跑通 |
| 第 3 周 | Qt 界面接入 + 测试完善 + 报告 + 预答辩 |

---

## 验收及格线（对照检查）

- [ ] CMake 一键编译无错误
- [ ] 支持 ≥10 条 RV32I 指令
- [ ] 斐波那契（或数组求和）程序结果正确
- [ ] GUI 能单步 / 运行 / 查看寄存器与内存
- [ ] 有测试用例与课程报告
