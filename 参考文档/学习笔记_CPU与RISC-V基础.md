# 学习笔记：CPU 模拟器与 RISC-V 基础

> 日期：2026-08-27 ｜ 主题：从零理解 CPU 如何工作 + RISC-V 指令编码 + 项目代码结构

---

## 一、CPU 是什么

CPU = 一个**照着菜谱做菜的厨师**，它的本质是"读一步、懂一步、做一步、手指往下移一步"的循环机器，每秒转几十亿次，**自身没有智能**，智能都在程序（菜谱）里。

| 比喻 | 对应 |
|---|---|
| 菜谱书 | 内存（存指令） |
| 厨师的手指 | PC（程序计数器，指向当前指令） |
| 手边小操作台 | 寄存器（32 个，快而小） |
| 远处储藏室 | 内存（大而慢） |

---

## 二、一切皆数字

- **程序 = 一串 32 位的数字**，一个数字 = 一条指令，这就是**机器码**；
- 文字 `addi x1, x0, 5` → 汇编器 → 数字 `0x00500093`；
- 每条指令占 **4 字节**（32 位 = 4 字节），所以下一条在"当前地址 +4"。

---

## 三、取指 → 译码 → 执行（循环）

`CPU::step()` 就三行：

```cpp
u32 inst = fetch();   // ① 取指：读 PC 指向的那条
pc_ += 4;             // ② 手指先移到下一条
execute(inst);        // ③ 译码 + 执行
```

`run()` 一直循环 `step()`，直到 `pc >= progEnd`（手指到封底）或触发保险丝 `maxSteps`。

- **pc** = 会动的手指；
- **progEnd** = 固定的终点（装程序时算好一次，之后不变）。

---

## 四、RISC-V 指令编码（切分）

一个 32 位数，按 **RISC-V 手册规定的图**切几段：

| 字段 | 位数 | 作用 |
|---|---|---|
| opcode | 7 位（最低） | 大类 / 格式 |
| funct3 | 3 位 | 小类 |
| funct7 | 7 位（最高，仅 R 型） | 兜底（区分 add/sub） |
| rd | 5 位 | **目的**寄存器（结果写到这） |
| rs1 / rs2 | 各 5 位 | **源**寄存器（从这里读） |
| imm | 立即数 | 直接写在指令里的常数 |

三种格式（现在只需认识这三种）：

```
R 型（opcode=OP）：     [funct7 7位][rs2 5位][rs1 5位][funct3 3位][rd 5位][opcode 7位]  → add/sub
I 型（opcode=OP_IMM）： [立即数 12位        ][rs1 5位][funct3 3位][rd 5位][opcode 7位]  → addi
U 型（opcode=LUI）：    [立即数 20位                 ][rd 5位][opcode 7位]               → lui
```

**关键：先读 opcode → 决定格式 → 再按格式切剩下位。**（就像文件后缀名决定解析方式）

---

## 五、x0 恒为 0

- **是 RISC-V 规范规定的，不是我们定的**（ISA 就是"合同"）；
- 代码用两个 `if (n == 0)` 实现：读返回 0、写直接丢弃；
- 设计动机：0 是最常用常数，省得每次"先装 0"。

---

## 六、项目架构（数据流拆分）

跟着数据流拆，每站一个类：

```
汇编文字 →[Assembler]→ 机器码 →[Memory]→ [CPU: 取指/执行] →[RegisterFile]→ 状态
```

| 类 | 职责 |
|---|---|
| common.h | 类型别名、常量、位操作工具 |
| RegisterFile | 32 个寄存器，x0 恒 0 |
| Memory | 字节寻址内存，小端读写 |
| CPU | 取指→译码→执行→写回 |
| Assembler | 汇编文字 ⇆ 机器码 |
| main.cpp | 胶水，串起上面四块 |

拆分原则：① 每站一模块；② 单一职责；③ 依赖单向；④ 公共的抽出来。

---

## 七、C++ 关键概念

- **类/对象**：类是模具，对象是造出来的实例；成员变量 = 对象自己的数据；
- **构造函数**：名字 = 类名，创建对象时自动调用；
- **参数**：函数里的占位符，调用时被实参填充；
- **const**：承诺"只读不改"；
- **std::array<类型,个数>** 和 **std::vector<类型>**：尖括号 `< >` 是模板参数；
- **类型别名**：`u8/u16/u32`（u=无符号，数字=位数）；`i32`（有符号）。

---

## 八、今天踩过的坑

1. **`u16` 未定义**（编译错误）—— 用了没定义的类型；看到 `XXX does not name a type` 就查是不是漏了定义或 include；
2. **`undefined reference`**（链接错误）—— 只编译了 main.cpp，没编译其他 .cpp；要用 CMake 编译全部文件并链接；
3. **中文乱码** —— UTF-8 源码 vs GBK 控制台，编码不匹配；只影响显示，不影响程序。

---

## 九、展望：表驱动 / 指令算子化

- 现在：`execute()` 用大 switch，加指令要改核心代码；
- 展望：把"一条指令"变成"标准算子"，用分派表组织（借鉴 buckyball 的 ball 抽象）；
- 表驱动 = 把"逻辑"变成"数据"；
- 成熟项目已实现：**Spike 一指令一文件（约 1000 个文件）**、qtrvsim 有完整文档 + 测试。

> 名言：**结构决定"能不能改"，测试决定"敢不敢改"，文档决定"会不会改"。**
