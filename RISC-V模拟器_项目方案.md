# RISC-V CPU 模拟器 · C++ 课程设计项目方案

> 版本 v1.0 ｜ 周期 3 周 ｜ 团队 6 人 ｜ 难度：中

---

## 1. 项目概述

**项目名称**：RISC-V CPU 模拟器（RV32I 子集）

**一句话定位**：用 C++ 实现一个软件版 RISC-V 处理器，读入汇编程序，逐条取指、译码、执行，输出寄存器与内存状态，并配一个图形界面（Qt）展示执行过程。

**为什么选它**：
- C++ 知识点全覆盖（类与对象、继承多态、模板、STL、文件流、位运算）；
- 逻辑单线程、确定性，好调试，3 周能出结果；
- 与团队已有的 RISC-V / 体系结构背景重合，事半功倍。

---

## 2. 项目目标

### 核心目标（及格线，必须完成）
1. 可一键编译（CMake）；
2. 支持 **≥10 条 RV32I 指令**（`addi/add/sub/lui/and/or/xor/sll/srl/beq/jal/lw/sw` 等）；
3. 能跑通**至少一个真实测试程序**（斐波那契 / 数组求和），结果正确；
4. 有 **Qt 图形界面**：单步 / 运行 / 重置 + 寄存器面板 + 内存面板；
5. 有**测试用例**与**课程报告**。

### 加分目标（可选，做成了是亮点）
- 5 级流水线（单周期 → 流水线）；
- 完整 RV32I 指令覆盖；
- 反汇编器；
- 与 Spike 官方模拟器自动化对拍；
- 标签跳转（汇编器支持 `loop:` 这种 label）。

---

## 3. 技术架构

### 3.1 数据流

```
汇编文本 ──Assembler──▶ 机器码 ──Memory──▶ CPU 取指/执行 ──▶ 寄存器/内存状态 ──Qt界面──▶ 屏幕
```

### 3.2 核心类（接口已定稿，见 riscv-sim/ 目录）

| 类 | 职责 | 关键接口 |
|---|---|---|
| `common.h` | 类型别名、opcode 常量、位操作工具 | `bits()` `signExtend()` `regNum()` |
| `RegisterFile` | 32 个寄存器，x0 恒 0 | `read(n)` `write(n,v)` |
| `Memory` | 字节寻址内存，小端读写 | `loadWord(addr)` `storeWord(addr,v)` |
| `CPU` | 取指→译码→执行→写回 | `loadProgram()` `step()` `run()` `pc()` `reg()` `mem()` |
| `Assembler` | 汇编文本 ⇆ 机器码 | `assembleLine()` `assemble()` `disassemble()` |

**接口冻结机制**：第 1 周结束时 `.h` 文件定稿，之后任何人改动接口必须全组同步。界面/测试只依赖 `CPU` 的三个只读接口 `pc()/reg()/mem()`，与核心实现完全解耦。

---

## 4. 分工（6 人）

| # | 角色 | 负责文件 | 职责 |
|---|---|---|---|
| 1 | **架构负责人**（你） | `common.h`、`CPU.h`、`CMakeLists.txt` | 定接口、管 git、集成、主笔报告 |
| 2 | **CPU 执行引擎**（最强程序员） | `CPU.cpp` | 实现 `execute()` 全部指令 |
| 3 | **汇编器** | `Assembler.cpp` | 汇编 + 反汇编 |
| 4 | **内存与加载** | `Memory.cpp` | 内存实现 + 程序加载 |
| 5 | **Qt 界面** | `gui/` | 4 面板 + 4 按钮 |
| 6 | **测试与验证** | `tests/` | 测试汇编程序 + 真值表 + 与 Spike 对拍 + 辅助报告 |

> 说明：`CPU.cpp` 只设一个 owner，避免两人同时改一个文件打架；测试岗不是打杂，是真活——写汇编测试程序、定"输入→期望输出"真值表、跟 Spike 对拍，工作量不小。

---

## 5. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | C++17 | 课程要求，现代特性够用 |
| 构建 | CMake | 统一构建，6 人协作不乱 |
| 界面 | Qt (QWidgets) | 老师未指定框架，Qt 文档好、打分友好 |
| 版本控制 | git + GitHub | 每人一个分支，合并走 PR |
| 参考基准 | Spike (riscv-isa-sim) | 官方模拟器，对拍验证结果 |

---

## 6. 三周进度计划

### 第 1 周：接口冻结 + 最小闭环
- **目标**：`.h` 接口定稿，最小 demo 跑通 `x3=12`
- **里程碑产物**：`riscv-sim` 能编译运行，`addi/add/sub/lui` 正确
- **各自任务**：架构定接口；执行引擎实现 4 条基础指令；汇编器支持这 4 条；内存实现 load/store；测试写真值表；界面先装 Qt 跑通空白窗口

### 第 2 周：功能补全 + 端到端
- **目标**：补全 RV32I 主体（分支/跳转/load-store/逻辑/移位），跑通斐波那契
- **里程碑产物**：`beq` 循环 + `lw/sw` 访存 + 逻辑指令全部正确，端到端跑通
- **各自任务**：执行引擎补指令；汇编器补指令 + 标签跳转；测试跑斐波那契对拍 Spike；界面画寄存器/内存面板

### 第 3 周：界面接入 + 打磨 + 报告
- **目标**：GUI 与核心对接，测试完善，写报告
- **里程碑产物**：可演示的完整程序 + 报告 + PPT
- **各自任务**：界面接 `cpu.step()/reg()/mem()`；测试补边界用例；架构集成 + 写报告；全组预答辩演练

---

## 7. 验收标准

### 及格线（必须全部满足）
- [ ] CMake 一键编译无错误
- [ ] 支持 ≥10 条 RV32I 指令
- [ ] 斐波那契（或数组求和）程序结果正确
- [ ] GUI 能单步 / 运行 / 查看寄存器与内存
- [ ] 有测试用例与课程报告

### 加分项（可选）
- [ ] 5 级流水线
- [ ] 完整 RV32I 覆盖
- [ ] 与 Spike 自动化对拍脚本
- [ ] 汇编器支持 label 跳转
- [ ] 反汇编器

---

## 8. 协作规范

1. **分支策略**：每人一个 `feat/xxx` 分支，合并走 Pull Request，由架构负责人 review；
2. **接口冻结**：第 1 周结束 `.h` 定稿，改接口必须全组同步；
3. **编码规范**：统一 `u32/i32/u8` 类型别名、4 空格缩进、中文注释（见 `common.h` 注释风格）；
4. **测试先行**：每条指令先定"输入→期望输出"真值表，再写实现；
5. **每日同步**：每天 10 分钟站会，说清"今天做什么、卡在哪"。

---

## 9. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| CPU.cpp 唯一 owner 掉链子 | 核心停滞 | 架构负责人兜底，测试岗提前介入 |
| 集成地狱（接口对不上） | 最后拼不起来 | 接口冻结 + 每日合并 + 第 2 周开始联调 |
| GUI 拖到最后一刻接不上 | 演示翻车 | 界面只依赖 3 个只读接口，核心可独立演示 |
| 有人搭便车 | 进度不透明 | git 提交记录做证据，分工到"人+模块+验收" |
| 指令编码写错 | 结果错难查 | 每条指令先与 Spike/绿卡核对编码 |

---

## 10. 参考资料

- **skyzh/RISCV-Simulator**（C++，RV32I，带报告）：https://github.com/skyzh/RISCV-Simulator
- **cvut/qtrvsim**（C++/Qt 教学模拟器，学 GUI）：https://github.com/cvut/qtrvsim
- **Spike**（官方模拟器，对拍基准）：https://github.com/riscv-software-src/riscv-isa-sim
- **RISC-V 绿卡 / 官方 ISA 手册**（指令编码权威）：riscv.org/specifications
- **CS61C 单周期数据通路笔记**：https://mdrkrg.github.io/notes/pages/coding/cs61c/src/cpu/Single-Cycle%20Datapath.html

---

## 附：起点工程

最小可跑工程已就绪（`riscv-sim/` 目录，12 个文件），当前已实现 `addi/add/sub/lui` 并跑通 `x3=12`。全员以此为起点认领模块。
