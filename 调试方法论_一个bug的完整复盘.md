# 调试方法论：一个 bug 的完整复盘

> 日期：2026-09-05 ｜ 素材：riscv-sim 数据通路图「只亮最后一条指令的灯」
> 定位：这不是坑的清单（那是《开发踩坑记录.md》），而是**一次错误诊断 + 一次正确诊断的对比**，用来提炼「怎么改错」的方法。

---

## 一、这个 bug 长什么样

**现象**：点「运行」，程序有 8 条指令，速度已经拉到很慢，但数据通路图上的灯**只亮一次**——就是最后一条指令该亮的那组。中间 7 条指令的灯从来没出现过。

单步（一下一下点）**完全正常**，每条指令的灯都对。

---

## 二、我的第一次诊断：错的

看到"运行看不到、单步看得到"，我的推理是：

> 单步正常 → 高亮逻辑本身是对的 → 那问题在运行路径 → 运行路径上次刚从 `cpu.run()` 改成 QTimer → 大概是没重新编译，跑的还是旧 exe。

然后我做了两件事，**两件事都做对了，但结论错了**：

1. `grep` 搜 `cpu.run()` —— 确认源码里没有残留的同步调用 ✅
2. 顺着读 `runTimer → onTimerTick → cpu.step() + refreshUI()` —— 确认链路完整 ✅

于是我告诉你"源码是对的，八成是旧 exe"，让你去自检。

**错在哪**：我验证的是「**代码有没有按我想的顺序执行**」，但真正的问题是「**代码按我想的顺序执行了，屏幕却没跟上**」。我把"执行了"和"看得见"当成了一回事。

---

## 三、真正的原因

罪魁祸首是这一行：

```cpp
void DatapathWidget::setHighlightMask(uint32_t mask) {
    m_mask = mask;
    update();          // ← 就是它
}
```

`update()` 的语义不是"现在重画"，而是"**往事件队列里塞一张重绘申请单**"。Qt 为了性能，会把队列里堆积的多张申请单**合并成一次真画**。

所以真实的执行时序是：

```
timer 第1拍 → cpu.step() → m_mask 改了 → update() → 申请单进队列
timer 第2拍 → cpu.step() → m_mask 又改了 → update() → 和上一张合并
timer 第3拍 → ...
        （事件循环一直在处理 timer，抽不出空处理重绘）
        ...
程序跑完 → 事件循环终于空了 → 处理那唯一一张合并后的申请单
                            → paintEvent() 读 m_mask
                            → 此时 m_mask 已经是最后一条指令的值
```

**8 拍改了 8 次 `m_mask`，但 `paintEvent()` 只被调了 1 次，读到的是最后那次的值。** 中间 7 个状态确实存在过，只是从来没被画出来。

单步为什么正常？因为你手动点按钮，两次点击之间有几百毫秒空闲，事件循环有充足时间把重绘申请单消化掉——**一拍一画，正好对上**。

### 修法

```cpp
// refreshUI() 末尾
datapath->repaint();   // repaint() 是同步的：立刻画完才返回
```

`repaint()` 绕过队列直接触发 `paintEvent()`，每一拍都强制落到屏幕上。

（还有个加剧因素：`refreshUI()` 里内存表每拍 `new` 几十个 `QTableWidgetItem`，占满了事件循环的时间。改成复用已有单元格 `it->setText()` 后，事件循环轻松很多。）

---

## 四、从这次错误里提炼的方法

### 方法 1：把"应该发生"和"确实发生"分开验证

我第一次诊断读代码读出的是「**应该**每拍刷一次」。但读代码只能证明**意图**，不能证明**结果**。

要证明结果，得让程序自己说话。这次修完我顺手加了两个"自证"机制：

```cpp
// 每拍往日志打一行
logEdit->append(QString("[%1] %2").arg(tickCount, 3).arg(describeInst(cpu.inst())));
// 图顶部显示「第 N 拍：addi x1, x0, 5（PC → 0x00000004）」
datapath->setInstText(...);
```

有了它们，同类怀疑一眼就能分诊：

| 日志行数 | 图上的拍数 | 灯在变吗 | 结论 |
|---|---|---|---|
| 只有 1 行 | 停在 1 | 不变 | CPU 真的只走了一步 → 查 `step()`/`finished()` |
| 有 8 行 | 数字在跳 | 不变 | 计算对、显示没跟上 → 查重绘 |
| 有 8 行 | 停在 1 | 不变 | 连文字都没刷 → 整个 widget 没重绘 |

**这就是把一个模糊的"看不到"劈成三种可区分的故障。** 加日志的成本是 5 分钟，省下的是反复猜。

### 方法 2：现象的边界就是线索

「运行不行、单步行」这句话里，**差异本身**就指向答案。

我当时用它推出了"问题在运行路径"（对），但没继续问下一层：**运行和单步的差别到底是什么？**

- 表面差别：一个是 timer 自动、一个是手点 → 指向"代码路径不同"（我走的这条，错了）
- 深层差别：**两次执行之间有没有空闲时间** → 指向"事件循环来不来得及" ✅

**遇到"A 行 B 不行"，要把 A 和 B 的差异列穷。** 只列到第一条就下结论，就会像我这样停在半路。

### 方法 3：别在"我熟悉的地方"找原因

我第一次把怀疑落在 QTimer 改动上，因为**那是我上次动过的地方**。这是个很自然但很坑的偏见——改动过的地方确实嫌疑最大，但当"改动本身经检查是正确的"之后，就该往外扩，去看那些**一直没动过、被当成理所当然的东西**。

`setHighlightMask()` 里的 `update()` 从队友第一版代码起就在那儿，从没改过，所以我下意识跳过了它。**恰恰因为一直没动，它才一直是错的。**

### 方法 4：查 API 的语义，不要凭名字猜

`update()` 这个名字看起来就是"更新一下"，很容易脑补成"立刻刷新"。实际 Qt 文档写得很清楚：

> `void QWidget::update()` — This function does not cause an immediate repaint; instead it **schedules** a paint event for processing when Qt returns to the main event loop.

一句 "schedules" 就是全部答案。**框架 API 里凡是涉及"什么时候生效"的，都要去确认是同步还是异步**——这类 bug 的表现（能算对但显示不对、快了出错慢了正常）几乎都是异步语义没搞清。

### 方法 5：承认第一次判断错了，别顺着它继续找证据

我说"八成是旧 exe"之后，如果你回来说"重编译了还是这样"，最省时间的做法是**推翻整条推理重来**，而不是继续在"编译"这个方向上加码（清缓存？删 build 目录？换 Release？）。

**一个错的方向能一直提供"看起来像进展"的动作。** 判断标准：如果新做的动作都是在给旧假设打补丁，而不是在做新的区分性验证，就该停下来换方向。

---

## 五、一套可复用的排查顺序

按这个顺序走，大部分"界面/显示不对"的问题能定位到：

1. **先分诊：是"没算"还是"没显示"？**
   往关键路径塞一行日志/`qDebug()`。这一步决定后面查哪半边，最省时间。
2. **列现象边界**：什么情况正常、什么情况不正常？把差异列穷（代码路径、时间间隔、数据规模、操作顺序…）。
3. **验证意图**：读代码确认链路（我第一次做的那步——**必要但不充分**）。
4. **验证结果**：让程序输出它实际走了几步、每步的值。意图和结果对不上的地方就是 bug。
5. **查 API 语义**：涉及"何时生效"的调用，确认同步/异步。
6. **改完复验**：不只看现象消失，还要能解释"为什么这么改就好了"。解释不通就说明只是碰巧盖住了。

---

## 六、同一天的另外两个错误（同类型）

这次的三个改动里，另外两个也值得记，因为它们暴露的是**同一类思维问题：结构没想清就动手画**。

### 1. `(pc/4) % 7` 的伪高亮

队友第一版用「第几条指令 → 第几个盒子」来点灯：

```cpp
int stage = (cpu.pc() / 4) % 7;   // 第 1 条亮第 1 个盒子，第 2 条亮第 2 个…
```

**为什么是错的**：这里的映射毫无物理含义。第 3 条指令亮"寄存器堆"，不是因为它用了寄存器堆，纯粹因为它排第 3。而且我们是**单周期 CPU**——一条指令在一拍里走完全部部件，根本不存在"这一拍在第 3 个阶段"这回事（那是五级流水线才有的概念）。

**修法**：改成按 `opcode` 算「这条指令真的用到哪些部件」，位掩码同时点亮多个：

```cpp
case Op::LOAD:   // lw：读寄存器算地址 → 访存 → 写回
    m |= STAGE_REG | STAGE_EX | STAGE_MEM | STAGE_WB;
case Op::STORE:  // sw：要访存，但不写回寄存器
    m |= STAGE_REG | STAGE_EX | STAGE_MEM;
```

**教训**：**做可视化之前，先说清"这个图在表达什么物理事实"。** 说不清就说明还没理解要画的东西，画出来只会是好看的错误。

### 2. 把 WB 画成一个盒子

第一版数据通路图是 7 个盒子一条直线：`PC → IF → ID → REG → EX → MEM → WB`。

**为什么是错的**：`WB`（写回）**不是一个部件，而是一个动作**——它是"把结果送回寄存器堆"这条**线**。画成盒子会让人以为存在一个叫"写回单元"的硬件，而且掩盖了真正的关键结构：**数据通路里有回路**。

单周期 CPU 有两条回路，第一版图两条都缺：
- **写回回路**：ALU/内存的结果绕回寄存器堆（`REG` 既是起点也是终点）
- **PC 回路**：分支/跳转把新地址送回 PC

**修法**：6 个部件排一行，两条回路分层走——写回总线在下方实线，PC 跳转在上方虚线（虚线表示"只在 beq/jal 时才有数据流"）。

**教训**：**画结构图先分清"名词"和"动词"。** 名词（PC、ALU、寄存器堆）画盒子，动词（取指、写回）画线或标注。混在一起画，图就会撒谎。

---

## 七、三条一句话总结

1. **"代码执行了" ≠ "屏幕看得见"** —— 验证意图之外还要验证结果，靠日志不靠推理；
2. **"A 行 B 不行"里的差异要列穷** —— 只列到第一条就下结论会停在半路；
3. **画图前先想清结构** —— 说不清图在表达什么物理事实，就还没到动手的时候。
